{{#paper-card}}
  {{#paper-card-content}}
    <h2>New Post</h2>
    <p>
      {{paper-input label="Post Title" value=title}}
      {{paper-input label="Post Description" value=description}}
      {{paper-input textarea=true label="Post" value=body attr-rows=3}}
    </p>
  {{/paper-card-content}}
  <div class="md-actions" layout="row" layout-align="end center">
    {{#paper-button primary=true action="post" primary=true}}Post{{/paper-button}}
    {{#paper-button primary=true action="cancel" warn=true}}Cancel{{/paper-button}}
  </div>
{{/paper-card}}